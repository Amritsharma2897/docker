{
  "name": "dummytesing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker-build-test": "docker build -f Dockerfile -t test305/automation:latest .",
    "docker-run-test": "docker run --rm -t --network host test305/automation:latest",
    "docker-compose-up": "docker-compose -f ./docker-compose.test.yml up --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.41.1",
    "@types/node": "^20.10.3"
  }
}
